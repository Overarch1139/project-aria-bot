TODOs:

- Move constant values & static vars to either an XML or JSON then parse that.
  (Should help with cleaning up the code)
- Setup testing environment
- Test out new timeout functionality
- Remove existing sensitive access credentials and set them as environment variables
